# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  - 0.10.25
  - 5

#matrix:
 # include:
  #  - node_js: 5
   #   env: tt@!=rr
      
build:

  ci:
    - ps -eax
    - ls -al
    #- if [ "$SHIPPABLE_NODE_VERSION" == "5" ]; then foobar; fi`
    
integrations:
  hub:
    - integrationName: ship-docker
      type: docker
  notifications:
    - integrationName: email
      type: email
      recipients: 
        - shiphitchcock1@gmail.com
        #- varsha@shippable.com
      on_start: always
      on_success: always  
      on_failure: always  
      on_pull_request: always  
